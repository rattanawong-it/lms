# Requirements Change Log

> ทุกการเปลี่ยนแปลง requirement, schema, โครงสร้าง หรือ tech stack ต้อง**แจ้งและได้รับอนุมัติก่อน**จึงจะบันทึกที่นี่ได้

| # | วันที่ | เอกสาร/ส่วน | การเปลี่ยนแปลง | เหตุผล | ผลกระทบ | สถานะ / ผู้อนุมัติ |
|---|---|---|---|---|---|---|
| 0 | 2026-09-19 | spec.md, system-design.md | สร้าง baseline v1.0 จากการสัมภาษณ์ requirements | เริ่มโครงการ | — | รอตรวจ |
| 1 | 2026-09-20 | system-design §11 | เพิ่ม `src/lib/roles.ts` (ส่วน RBAC ที่ client ใช้ร่วมได้) และให้ `lib/rbac.ts` re-export ต่อ | Client component ห้าม import โมดูล `server-only` — ทำให้ `pg`/`next/headers` หลุดเข้า browser bundle และ build ล้มทั้งหมด | โครงสร้าง `lib/` เพิ่ม 1 ไฟล์ · จุดตรวจสิทธิ์ยังอยู่ที่ `rbac.ts` เหมือนเดิม | อนุมัติแล้ว |
| 2 | 2026-09-20 | system-design §4.1, §11 | เพิ่ม `src/lib/permissions.ts` ประกาศ access control 4 บทบาทให้ Better Auth admin plugin | better-auth 1.7 บังคับว่าทุกค่าใน `adminRoles` ต้องมีใน `roles` มิฉะนั้น throw ตอน init | ไม่เปลี่ยน permission matrix — เป็นการเขียน matrix เดิมให้ plugin รู้จัก | อนุมัติแล้ว |
| 3 | 2026-09-20 | system-design §10.1 | เพิ่ม env `SEED_ADMIN_EMAIL`, `SEED_ADMIN_PASSWORD` | ใช้กำหนดบัญชี Super Admin ตั้งต้นของ `prisma/seed.ts` โดยไม่ hardcode รหัสผ่าน | เพิ่ม 2 ตัวแปรใน `.env.example` | อนุมัติแล้ว |
| 4 | 2026-09-20 | system-design §11, §9 | เพิ่ม `tests/unit/`, `tests/e2e/`, `vitest.config.ts`, `playwright.config.ts`, `.github/workflows/ci.yml` | ปิดงาน §12 ข้อ 4 (CI: lint, typecheck, test) ก่อนเริ่ม Phase 1 | เพิ่มขั้นตอน CI บน push/PR | อนุมัติแล้ว |
| 5 | 2026-09-20 | โครงสร้างโปรเจกต์ | `.gitignore`: ยกเว้น `.env.example` ออกจาก `.env*` และ ignore `/src/generated` · ESLint ignore `project-ui/**`, `src/generated/**` | `.env*` กลืน `.env.example` ทำให้ commit ไม่ได้ ขัดกับ §9 (Secrets) · ไฟล์จาก design tool และ Prisma client ไม่ควรถูก lint | `.env.example` เข้า repo ได้ · lint เหลือ 0 error | อนุมัติแล้ว |
| 6 | 2026-09-20 | spec §7 (D-01), system-design §10 | Storage: ใช้ MinIO ตลอด Phase 1 แล้วสลับเป็น Cloudflare R2 ตอน deploy ด้วยการเปลี่ยน `S3_*` · Hosting ยังไม่เลือก | เริ่ม M05 ต้องมีที่เก็บไฟล์จริง และ MinIO มีใน docker-compose อยู่แล้ว | `src/lib/storage.ts` ต้องเป็นที่เดียวที่รู้จัก endpoint/bucket · เพิ่ม env `S3_FORCE_PATH_STYLE` | อนุมัติแล้ว |
| 7 | 2026-09-20 | spec §7 (D-04), system-design §10 | วิดีโอ Phase 1 ใช้ MP4 ไฟล์เดียว (progressive + HTTP range) ยังไม่ transcode/HLS | เลี่ยง infra เพิ่ม (worker/ffmpeg) ในเฟส MVP · `Asset` รองรับการเพิ่ม rendition ภายหลังได้ | ผู้เรียนเน็ตช้าจะโหลดช้า — รับไว้เป็นข้อแลกเปลี่ยน | อนุมัติแล้ว |
| 8 | 2026-09-20 | docs/phase-1-plan.md | เพิ่มเอกสารแผนงาน Phase 1 (7 ขั้น, จุดหยุดตรวจ 2 จุด, ความเสี่ยง) | ทำตามลำดับ spec → design → ตรวจ → implement | ยังไม่มีผลต่อโค้ด | **ร่างรออนุมัติ** |
| 9 | 2026-09-21 | spec.md §3 (โมดูลใหม่ M00), §6, docs/phase-1-plan.md §3, `CLAUDE.md` | เพิ่มโมดูล **M00 Project Docs & Conventions** (FR-00.1–FR-00.5) และเขียน `CLAUDE.md` ที่รากโปรเจกต์ให้เป็น "สมองของโปรเจกต์" — context, conventions, คำสั่งที่ใช้บ่อย, สถาปัตยกรรมย่อ | เจ้าของระบบสั่งให้เอกสารกำกับโปรเจกต์เป็นรายการที่ติดตามได้ · แยกเป็น M00 แทนการยัดใน M01 เพื่อไม่ให้ checklist ของ Authentication ปนกับ deliverable ของทีมพัฒนา | ไม่กระทบโค้ดฟีเจอร์/schema · FR-00.5 เพิ่มภาระต่อเนื่อง: คอมมิตที่เปลี่ยน convention/คำสั่ง/โครงสร้าง ต้องอัปเดต `CLAUDE.md` ด้วย | อนุมัติแล้ว (2026-09-21) |
| 10 | 2026-09-21 | spec §4 (NFR-09), system-design §2, §8.1 | เปลี่ยนฟอนต์ในเอกสารจาก IBM Plex Sans Thai เป็น **Anuphan (ไทย) + Inter (อังกฤษ/ตัวเลข) + JetBrains Mono (โค้ด)** | โค้ดตั้งแต่ Phase 0 ใช้ 3 ฟอนต์นี้จริงมาตลอด (`src/app/layout.tsx`, `globals.css`) เอกสารตกค้างจาก baseline | แก้เอกสารให้ตรงโค้ด ไม่แตะโค้ดและไม่กระทบหน้าจอที่ทำไปแล้ว | อนุมัติแล้ว (2026-09-21) — เจ้าของระบบสั่ง "แก้ spec ตามโค้ด" |
| 11 | 2026-09-22 | docs/phase-1-plan.md §2 (ขั้น 5–6) | ขั้น 5 สร้าง **โครงหน้าเรียน** `/learn/[courseId]/[lessonId]` แบบ server-rendered (สารบัญ, ล็อกตามลำดับ, ปุ่ม "เรียนจบบทนี้") ล่วงหน้าจากขั้น 6 · ขั้น 6 ยังเป็นผู้เพิ่ม player, pdf.js, `<ProtectedViewer>` และ signed URL ตามเดิม | FR-06.3/06.4/06.5 ต้องมีหน้าให้กดและปลายทางให้ปุ่ม "เรียนต่อ" จึงจะทดสอบได้จริงตั้งแต่ขั้น 5 มิฉะนั้นต้องรอพิสูจน์ทั้งหมดในขั้น 6 | ขั้น 6 เปลี่ยนจาก "สร้างหน้าเรียน" เป็น "เติมสื่อและการป้องกันลงในหน้าที่มีอยู่" · ขอบเขตรวมของเฟสเท่าเดิม | อนุมัติแล้ว (2026-09-22) — เจ้าของระบบเลือกตัวเลือก "ทำโครงหน้าเรียนแบบง่ายก่อน" |
| 12 | 2026-09-22 | ไม่เปลี่ยน requirement — บันทึกการตัดสินใจ | เขียนแถว `Notification` ตั้งแต่ M06 (ลงทะเบียน/อนุมัติ/ปฏิเสธ/เพิ่มเป็นกลุ่ม/เรียนจบ) ทั้งที่หน้าจอแจ้งเตือนอยู่ M11 ขั้น 7 | ตรงกับ flow §5.3 ที่ระบุ "create Enrollment + Notification" · ขั้น 7 จะได้แค่เอามาแสดง ไม่ต้องย้อนแก้ `features/enrollment/actions.ts` | มีแถวใน `Notification` ก่อนมีหน้าจออ่าน — ไม่กระทบผู้ใช้ | อนุมัติแล้ว (2026-09-22) — เจ้าของระบบเลือก "เขียนแถวลง DB เลย" |
| 13 | 2026-09-22 | tech stack (package.json) | เพิ่ม dependency **`pdfjs-dist`** | FR-05.3 ระบุให้แสดง PDF ด้วย pdf.js แบบ canvas โดยตรง ไม่มีทางเลือกอื่นที่ตรงตาม requirement | เพิ่มไลบรารีฝั่ง client 1 ตัว · worker ถูก bundle ผ่าน `new URL(...)` ไม่ต้องคัดลอกไฟล์เข้า `public/` | อนุมัติแล้ว (2026-09-22) |
| 14 | 2026-09-22 | system-design §5.3, §6.1 | **PDF ของบทเรียนเสิร์ฟผ่าน `/api/lesson-media/[lessonId]` (stream + HTTP Range) แทน signed URL ตรงไป storage** · วิดีโอยังใช้ signed URL ตามเดิม | pdf.js ดึงไฟล์ด้วย fetch จึงติด CORS เมื่อข้ามโดเมน · ผ่าน route ของระบบได้ประโยชน์เพิ่ม: object key ไม่หลุด และตรวจ enrollment ใหม่ทุก request ไม่ใช่แค่ตอนออก URL · PDF เพดาน 50 MB จึงไม่หนักเท่าวิดีโอ (2 GB) ที่ต้องเลี่ยงการวิ่งผ่านเซิร์ฟเวอร์ | bandwidth ของ PDF วิ่งผ่านเซิร์ฟเวอร์แอป — รับไว้เป็นข้อแลกเปลี่ยน · FR-15.7 ยังถือว่าเข้าเงื่อนไข เพราะเข้มกว่า signed URL ทุกด้าน | อนุมัติแล้ว (2026-09-22) — เจ้าของระบบสั่งให้เสนอแนวทางที่ปลอดภัยและไม่กินทรัพยากรเซิร์ฟเวอร์ |
| 15 | 2026-09-22 | system-design §4.2, §9 · `proxy.ts` | `proxy.ts` รับหน้าที่เพิ่ม: ออก **nonce และ Content-Security-Policy** ต่อ request (เดิมมีแต่ security headers คงที่ใน `next.config.ts`) | แผนขั้น 6 ระบุว่าต้องเพิ่ม CSP · วิธีที่คู่มือ Next 16 แนะนำคือออก nonce จาก proxy เพราะ CSP ต้องเปลี่ยนทุก request | ทุกหน้าเป็น dynamic อยู่แล้วจึงไม่เสีย static optimization · ต้องส่ง nonce ให้ `next-themes` ด้วย · เพิ่มปลายทางฝัง iframe หรือโดเมนสื่อใหม่เมื่อไร ต้องแก้ CSP ด้วยเสมอ | อนุมัติแล้ว (2026-09-22) — เจ้าของระบบเลือก "nonce-based แบบเข้ม" |
| 16 | 2026-09-22 | system-design §11 (โครงสร้าง `src/lib/`) | เพิ่ม **`src/lib/rate-limit.ts`** — ตัวจำกัดความถี่แบบ fixed window ในหน่วยความจำ | FR-15.8 บังคับให้ `/api/events/screen` มี rate limit และการขอ signed URL ของวิดีโอก็ต้องจำกัดเช่นกัน · rate limit ของ Better Auth ใช้ได้เฉพาะ endpoint ของตัวเอง | นับแยกต่อ process — ถ้า deploy หลาย instance ต้องย้ายไปใช้ Redis (บันทึกไว้ในไฟล์แล้ว) | อนุมัติแล้ว (2026-09-22) |
| 17 | 2026-09-22 | `vitest.config.ts`, `prisma/seed.ts` | vitest alias `server-only` ไปที่ไฟล์เปล่า · seed เพิ่มบทเรียน "วิดีโออัปโหลด (ไฟล์ตัวอย่าง เล่นจริงไม่ได้)" พร้อม MP4 จิ๋ว | โมดูลฝั่ง server ที่เป็นตรรกะล้วน (เช่น `lib/rate-limit.ts`) ทดสอบไม่ได้เพราะ `server-only` throw ใน jsdom · เส้นทาง signed URL ของวิดีโอไม่มีข้อมูลตัวอย่างให้ e2e ตรวจ | ไม่กระทบโค้ดที่รันจริง · ไฟล์ MP4 ตัวอย่างเล่นไม่ได้โดยตั้งใจ และตั้งชื่อบทเรียนบอกไว้แล้ว | อนุมัติแล้ว (2026-09-22) |
| 18 | 2026-09-23 | system-design §4.3, §7, §11 · spec §3.0.1 | **เพิ่ม route `/announcements`** (ผู้ใช้ที่ login อ่านประกาศที่ส่งถึงตน ปักหมุดขึ้นก่อน) และ `/admin/announcements` (ประกาศทั้งระบบ/ระดับคณะ) · **เผยแพร่ประกาศแล้วสร้างแถว `Notification` ให้ผู้รับทุกคน** ลิงก์ไปที่ `/announcements#a-<id>` · โฟลเดอร์ฟีเจอร์ใช้ชื่อพหูพจน์ `features/announcements`, `features/notifications` ให้ตรงกับ `departments`/`users`/`categories` ที่มีอยู่ | route map เดิมไม่มีที่ให้ผู้เรียนอ่านประกาศ และหน้าหลักจะยาวเกินถ้าแสดงทั้งหมด · สร้างแถวต่อผู้รับทำให้ตัวเลขบนกระดิ่งนับจากตารางเดียวด้วย index `[userId, readAt]` และตรงกับสถาปัตยกรรม §7 ที่เฟส 3 จะต่อ email/LINE | ประกาศทั้งระบบ = 1 แถวต่อผู้ใช้ 1 คน (INSERT ชุดละ 1,000 แถว) · ระดับคณะไม่รวมคณะย่อย ตามขอบเขตของ Dept Admin เดิม · ลบประกาศแล้วลบการแจ้งเตือนที่ชี้มาด้วย | อนุมัติแล้ว (2026-09-23) — เจ้าของระบบเลือก "สร้างแถวให้ผู้รับทุกคน" และ "เพิ่มหน้า /announcements" |
| 19 | 2026-09-23 | docs/phase-2-plan.md | เพิ่มเอกสารแผนงาน Phase 2 (ขั้น 0 spike PDF + 6 ขั้น, จุดหยุดตรวจ 2 จุด) พร้อมข้อเสนอเปลี่ยน schema S1–S4 และ route ใหม่ 4 เส้นทาง | ทำตามลำดับ spec → design → ตรวจ → implement ก่อนเริ่ม M07–M10 | ยังไม่มีผลต่อโค้ด · schema/route/dependency บันทึกแยกเป็นข้อ 20 | อนุมัติแล้ว (2026-09-23) — Q1–Q3, Q7–Q9 ตามข้อเสนอ · Q4 ใช่ · Q5 รองรับทั้ง CSV และ .xlsx · Q6 เกณฑ์ตัดเกรดจริงจะส่งให้ก่อนจบเฟส |
| 20 | 2026-09-23 | Prisma schema, system-design §3.2, §4.3 · tech stack | **schema:** `Question.archivedAt`, `Course.gradeScale`, `Grade.overridden`, `Submission.returnedAt` · **route:** `/teach/courses/[id]/questions`, `/learn/[courseId]/grades`, `/api/submission-file/[assetId]`, `/api/certificate/[code]` · **dependency:** `exceljs` (นำเข้า/ส่งออก .xlsx ฝั่ง server) | ลบข้อสอบจริงจะลบคำตอบเก่าของผู้เรียนตาม (cascade) · FR-09.2 ต้องมีที่เก็บเกณฑ์ตัดเกรด · คะแนนที่ผู้สอนแก้ต้องไม่ถูกผลสอบใหม่ทับ · FR-08.4 ส่งกลับให้แก้ต้องเปิดส่งใหม่ได้ · เจ้าของระบบต้องการทั้ง CSV และ Excel (FR-07.7, FR-09.5) | migration เพิ่มคอลัมน์ 4 ตัว (nullable/default ทั้งหมด ไม่กระทบข้อมูลเดิม) — ทำพร้อมขั้นที่ใช้ · `@react-pdf/renderer` + `qrcode` ยืนยันแล้วจาก spike ขั้น 0 (ต้องแตก `ำ` ก่อนเรนเดอร์ — phase-2-plan ขั้น 0) | อนุมัติแล้ว (2026-09-23) — Q5, Q9 ใน phase-2-plan §6 |
| 21 | 2026-09-23 | system-design §11 (โครงสร้าง `src/lib/`) | เพิ่ม `src/lib/csv.ts` (CSV ตาม RFC 4180 อ่าน/เขียน + BOM), `src/lib/xlsx.ts` (server-only, ครอบ `exceljs`), `src/lib/decimal.ts` (Decimal → number) · `RichTextField` ส่งค่าเดิมระหว่างที่ editor ยังโหลด | ตัวอ่าน CSV เดิมใน `features/users` ตัดตามบรรทัด จึงอ่านเซลล์ที่ขึ้นบรรทัดใหม่ (โจทย์หลายบรรทัด) ไม่ได้ · ขั้น 5 ต้องส่งออก CSV/XLSX ด้วยตัวเดียวกัน · คะแนนเป็น `Decimal` ส่งเข้า Client Component ตรง ๆ ไม่ได้ · placeholder เดิมไม่มีช่องซ่อน ทำให้บันทึกเร็วแล้วเนื้อหาถูกล้าง | โครงสร้าง `lib/` เพิ่ม 3 ไฟล์ · ตัวอ่าน CSV ของการนำเข้าผู้ใช้ยังเป็นตัวเดิม (ย้ายมาใช้ตัวใหม่ได้ภายหลัง) · แก้บั๊กที่กระทบฟอร์มคอร์ส/บทเรียนเดิมด้วย | รับทราบแล้ว (2026-09-23) — เจ้าของระบบให้ดำเนินการขั้น 2 ต่อ |
| 22 | 2026-09-23 | system-design §4.2, §4.3 · โครงสร้าง `features/enrollment` | (1) `assertCourseAccess(…, "learn")` รับ enrollment สถานะ `COMPLETED` ด้วย (เดิมรับเฉพาะ `ACTIVE`) · (2) ย้าย `writeProgress()` ไป `features/enrollment/lib/progress-writer.ts` (server-only) · (3) route ย่อย `/teach/courses/[id]/quizzes/{new,[quizId]}` ใต้ `quizzes` ที่อนุมัติไว้ | (1) บั๊กจาก Phase 1: ผู้เรียนที่เรียนจบคอร์สเปิดบทเรียนได้ 403 ทั้งที่หน้าคอร์สแสดงปุ่ม "ทบทวนบทเรียน" · (2) การสอบผ่าน (M07) ต้องบันทึกบทเรียนจบของผู้เรียนได้ แม้ผู้ที่ทำให้ attempt ถูกปิดจะเป็นผู้สอน · (3) หน้าตั้งค่าแบบทดสอบต้องมี URL ของตัวเอง | ผู้เรียนที่จบแล้วเข้าทบทวนเนื้อหาและทำแบบทดสอบที่ยังเหลือสิทธิ์ได้จนถึงวันหมดอายุ · ไม่เปลี่ยนสูตรความคืบหน้า | แจ้งในรายงานขั้น 2 (2026-09-23) — รอเจ้าของระบบรับทราบ |
| 23 | 2026-09-23 | system-design §4.3 · พฤติกรรม M07 | (1) route ย่อยใต้ `quizzes` ที่อนุมัติไว้: `/teach/courses/[id]/quizzes/review` (คิวรอตรวจ), `quizzes/[quizId]/results` (ผลสอบ), `quizzes/[quizId]/attempts/[attemptId]` (คำตอบรายคน + ตรวจ) · (2) ข้ออัตนัยที่ไม่ได้ตอบ/ตอบว่างได้ 0 ทันทีตอนส่ง · (3) ผู้เรียนเห็นคะแนนและความเห็นของข้ออัตนัยที่ตรวจแล้วแม้ตั้งค่าไม่แสดงเฉลย · (4) ผลที่ผู้สอนแก้จากผ่านเป็นไม่ผ่านไม่ย้อนความคืบหน้าที่บันทึกไปแล้ว | (1) FR-07.4 ต้องมีคิวรอตรวจและหน้าผลสอบ — route map มีแค่ `quizzes` · (2) เดิม attempt ค้าง "รอตรวจอัตนัย" ตลอดไปโดยไม่มีคำตอบให้ตรวจ · (3) คะแนนที่ผู้สอนให้ไม่ใช่เฉลย และ feedback ไร้ประโยชน์ถ้าผู้เรียนไม่เห็น · (4) การถอนการจบคอร์สผูกกับใบประกาศ (M10) | ไม่เปลี่ยน schema · (4) ต้องตัดสินใจอีกครั้งในขั้น 6 ว่าจะเพิกถอนใบประกาศ/การจบคอร์สเมื่อคะแนนถูกแก้ลงหรือไม่ | แจ้งในรายงานขั้น 3 (2026-09-23) — รอเจ้าของระบบรับทราบ |
| 24 | 2026-09-23 | system-design §4.3, §6 · Prisma schema (S4) · พฤติกรรม M08 | (1) migration S4 `Submission.returnedAt` ตามที่อนุมัติ (#20) · (2) route ย่อยใต้ `assignments`: `new`, `[assignmentId]`, `[assignmentId]/submissions`, `[assignmentId]/submissions/[submissionId]`, `review` · (3) `/api/upload/presign` รับ `assignmentId` — ผู้เรียนอัปโหลดได้เฉพาะไฟล์ส่งงาน · `/api/upload/{part,complete}` ตรวจแค่ความเป็นเจ้าของไฟล์ที่ค้าง (สิทธิ์ตัดสินแล้วตอน presign) · (4) ส่งใหม่หลัง "ส่งกลับให้แก้" ใช้สถานะ "ส่งช้า" ตามงานที่ถูกส่งกลับ · (5) ตรวจได้เฉพาะการส่งครั้งล่าสุด ส่งแล้วตรวจแล้วปิดรับ · (6) ขนาดไฟล์ส่งงานสูงสุด 25 MB, 10 ไฟล์ต่อครั้ง | (2) หน้าตั้งค่า/ตรวจงานต้องมี URL ของตัวเอง (แบบเดียวกับ `quizzes` — #22, #23) · (3) FR-08.2 ต้องให้ผู้เรียนแนบไฟล์ โดยไม่เปิดสิทธิ์อัปโหลดทั่วไป (phase-2-plan ขั้น 4) · (4) FR-08.3 บอกแค่ `submittedAt > dueAt` — ถ้าใช้ตรง ๆ งานที่ผู้สอนขอให้แก้หลังกำหนดจะกลายเป็น "ส่งช้า" ทั้งที่ส่งเดิมทันเวลา · (6) ใช้เพดานไฟล์ชนิด `FILE` เดิมของระบบ | ผู้เรียนมีช่องทางอัปโหลดใหม่ — จำกัดด้วยงาน/enrollment/นามสกุล/ขนาด/magic bytes/rate limit 30 ไฟล์ต่อ 10 นาที | แจ้งในรายงานขั้น 4 (2026-09-23) — รอเจ้าของระบบรับทราบ |
| 25 | 2026-09-23 | Prisma schema (S2, S3) · system-design §4.3, §11 · พฤติกรรม M09 | (1) migration S2 `Course.gradeScale` + S3 `Grade.overridden` ตามที่อนุมัติ (#20) · (2) route ย่อย `/teach/courses/[id]/gradebook/settings` · (3) แบบทดสอบลงสมุดเป็นร้อยละของครั้งที่ดีที่สุด (เต็ม 100) · (4) ช่องที่ยังไม่มีคะแนนนับเป็น 0 · ตัดเกรดจากคะแนนรวมที่ปัด 2 ตำแหน่งแล้ว · (5) เงื่อนไขจบ `minScore` เริ่มบังคับใช้ — คอร์สที่ตั้งไว้แต่น้ำหนักไม่ครบ 100% ผู้เรียนจะยังจบไม่ได้ · (6) ส่งออกผ่าน server action (base64) ไม่เพิ่ม route · ย้าย `saveBase64` ไป `src/components/shared/save-file.ts` | (2) แยกหน้าตั้งค่าออกจากตาราง ตารางจะได้ไม่ยาวเกินบนมือถือ · (3) คะแนนเต็มของแต่ละครั้งต่างกันได้เพราะสุ่มข้อจากคลัง · (4) ตัวเลขที่ผู้เรียนเห็นต้องเป็นตัวเลขเดียวกับที่ใช้ตัดเกรด (phase-2-plan §7) · (5) Q4 · (6) ใช้รูปแบบเดียวกับแม่แบบนำเข้าข้อสอบ | ไม่มีตารางใหม่ · คอร์สเดิมที่ตั้ง `minScore` ไว้ตั้งแต่ Phase 1 จะถูกตัดสินจบด้วยคะแนนรวมตั้งแต่นี้ · เกณฑ์ตัดเกรดยังเป็นค่าชั่วคราว (Q6) | แจ้งในรายงานขั้น 5 (2026-09-23) — รอเจ้าของระบบรับทราบ |
| 26 | 2026-09-23 | spec FR-10.5 · system-design §4.3, §5.6, §11 · tech stack · พฤติกรรม M10 | (1) **ใบประกาศที่ออกแล้วไม่ถูกเพิกถอนอัตโนมัติ** เมื่อคะแนนถูกแก้จนผู้เรียนหลุดเงื่อนไขจบ — เพิกถอนได้โดยผู้ดูแลเท่านั้น · (2) route: `/teach/courses/[id]/certificate` (แม่แบบ), `/admin/certificates` (ค้นหา/เพิกถอน) · (3) โฟลเดอร์ใหม่ `assets/fonts/anuphan/` (TTF + OFL) · dependency `@react-pdf/renderer` 4.9.0, `qrcode` 1.5.4 ตามที่อนุมัติ (#20) · `lib/storage.ts` เพิ่ม `putObject`/`readObject` · (4) หน้า verify ไม่แสดงเหตุผลการเพิกถอน, `noindex`, จำกัด 30 ครั้ง/นาที/IP · ใบที่ถูกเพิกถอนดาวน์โหลดไม่ได้ · (5) ผู้ดูแลคณะ (DEPT_ADMIN) เพิกถอนได้เฉพาะคอร์สของคณะตัวเอง · (6) คอร์สที่จบก่อนมี M10 ออกใบให้ตอนผู้เรียนเปิด `/certificates` · (7) โฟลเดอร์ฟีเจอร์ชื่อพหูพจน์ `features/assignments`, `features/certificates` (ตาม #18) | (1) เจ้าของระบบตัดสิน 2026-09-23 (ค้างจากขั้น 3) · (2) FR-10.2 ต้องมีที่แก้แม่แบบ, FR-10.5 ต้องมีที่ให้ผู้ดูแลเพิกถอน — route map ยังไม่มีทั้งคู่ · (3) ตัว render ฝั่ง server ใช้ `next/font` ไม่ได้ · ต้องเขียนไฟล์ที่ server สร้างเองลง storage · (4) หน้าสาธารณะที่มีชื่อบุคคล — ไม่ควรเปิดเผยเหตุผลและไม่ควรให้ไล่สุ่มรหัสได้ง่าย · (5) ตรงกับขอบเขตสิทธิ์ `manage` เดิม | ขอบเขตงานเท่าแผน · แก้บั๊ก Phase 1 เพิ่ม: จบคอร์สด้วยการดูวิดีโอไม่ส่งแจ้งเตือน/ไม่ออกใบ | (1) อนุมัติแล้ว (2026-09-23) · (2)–(7) แจ้งในรายงานขั้น 6 — รอเจ้าของระบบรับทราบ |
| 27 | 2026-09-23 | โครงสร้างโปรเจกต์ · system-design §11 · `CLAUDE.md` §3 | เพิ่ม **pre-commit hook** `.githooks/pre-commit` (lint → `next typegen` → typecheck → unit test) · ติดตั้งผ่านสคริปต์ `prepare` (`scripts/install-git-hooks.mjs` → `git config core.hooksPath .githooks`) ไม่เพิ่ม dependency · `.gitattributes` ทำเครื่องหมาย `*.ttf` เป็นไบนารี | เจ้าของระบบต้องการให้ทดสอบก่อน commit ทุกครั้งแบบบังคับ ไม่ขึ้นกับว่าคนหรือ AI จำได้ (เลือกแบบเบา: ไม่รวม e2e) | commit ช้าลงราว 1–2 นาที · e2e ยังรันใน CI และรันเองก่อนปิดขั้นงาน · ใครที่ clone แล้ว `pnpm install` ได้ hook อัตโนมัติ | อนุมัติแล้ว (2026-09-23) — เจ้าของระบบเลือก "hook แบบที่ 1" |
| 28 | 2026-09-23 | spec FR-04.1 (หน้า `/teach`) · ชุดทดสอบ e2e | (1) หน้าห้องผู้สอน `/teach` มีช่องค้นหาคอร์สจากชื่อ (`?q=`, ไม่สนตัวพิมพ์, อยู่ในขอบเขตคอร์สที่ผู้ใช้ดูแลได้เท่าเดิม) · (2) e2e เปิดคอร์สฝั่งผู้สอนผ่าน `teachSearch()` ใน `tests/e2e/helpers.ts` แทนการไล่หาในรายการทั้งหมด · (3) ล้าง DB พัฒนาบนเครื่อง (`prisma migrate reset` + seed + storage:init) | รันทั้งชุดแบบเปิดเบราว์เซอร์แล้วเทสต์ล้ม 2 ข้อเพราะหน้า `/teach` โหลดรายการ 180 คอร์สที่สะสมจากการทดสอบไม่ทัน · ผู้สอนที่มีคอร์สมากก็หาคอร์สยากเช่นกัน | ไม่เปลี่ยน schema/สิทธิ์ · ข้อมูลทดสอบเดิมใน DB พัฒนาหายหมด (ไฟล์ใน MinIO ยังค้างอยู่) | อนุมัติแล้ว (2026-09-23) — เจ้าของระบบสั่ง "ทำตามแนะนำ" และยืนยันการล้าง DB |
| 29 | 2026-09-24 | spec FR-09.6–09.9 · system-design §3.2, §3.3, §4.1, §4.3 · Prisma schema (S5, S6) · phase-2-plan ขั้น 7 | (1) **Score Curve** — เกณฑ์ 2 ส่วน: เกรด A–F และผ่าน/ไม่ผ่าน S/U แต่ละแถวมี Min/Max แก้ได้ (ทศนิยม ≤ 2) · ตรวจ 0–100, Min ≤ Max, ไม่ซ้อนทับ, จำนวนเต็มทุกค่ามีช่วง ก่อนบันทึก · (2) เกณฑ์ 3 ชั้น คอร์ส → คณะ (DEPT_ADMIN คณะตน) → ทั้งระบบ (SUPER_ADMIN) ที่ `/admin/score-curve` · (3) S5 ตาราง `ScoreCurve` · S6 `enum GradingMode` + `Course.gradingMode` (คอร์สเลือกตัดผลแบบเกรดหรือ S/U) · `Course.gradeScale` เปลี่ยนเป็นรูปแบบ `{grades,passFail}` (อ่านแบบเก่าได้) · (4) **ตัดผลจากคะแนนรวมที่ตัดทศนิยมทิ้ง** (แสดง 79.50 ตัดด้วย 79 → B+) แทนการเทียบค่าขั้นต่ำเดิม · (5) ค่าเกณฑ์ย้ายจากค่าคงที่ในโค้ด (`DEFAULT_GRADE_SCALE` ที่หน้าเว็บ import) ไปอยู่ใน DB | Q6 เจ้าของระบบส่งเกณฑ์จริงพร้อมขอหน้าตั้งเกณฑ์ · ต้องไม่ฝังค่าในหน้าเว็บ · คณะต่างกันอาจใช้เกณฑ์ต่างกัน | เกรดของคอร์สที่ไม่ได้ตั้งทับคำนวณตามเกณฑ์กลางใหม่ทันที (คำนวณตอนแสดง) · ใบประกาศที่ออกแล้วไม่ถูกกระทบ (#26) · เงื่อนไขจบ `minScore` ใช้ตามเดิมทั้งสองโหมด (Q12) | ✅ อนุมัติ 2026-09-24 — เจ้าของระบบ (Q10–Q13 ตามข้อเสนอ) |
| 30 | 2026-09-24 | docs/phase-3-plan.md · spec §3.0.1, §6 | เพิ่มเอกสารแผนงาน Phase 3 (ขั้น 0 เก็บงานค้าง + 7 ขั้น, จุดหยุดตรวจ 2 จุด) · `main` fast-forward ถึง `cfa76a7` และแตก branch `phase-3` | ทำตามลำดับ spec → design → ตรวจ → implement ก่อนเริ่ม M11 (อีเมล), M12–M14, M16, M17 | ยังไม่มีผลต่อโค้ด · schema/route/dependency บันทึกแยกเป็นข้อ 31 | อนุมัติแล้ว 2026-09-24 — เจ้าของระบบ (คำตอบ phase-3-plan §6) |
| 31 | 2026-09-24 | spec FR-17.3, FR-17.4 · Prisma schema · system-design §3.2, §4.3, §7 · tech stack | **schema:** S1 `Notification.dedupeKey` + unique `[userId, dedupeKey]` · S2 `Post.parent` self-relation, `Thread.updatedAt/lastPostAt`, `Post.updatedAt` + index · S3 `Review.repliedAt` + index + CHECK rating 1–5 · S4 `Course.ratingAvg/ratingCount` · S5 `User.deletionRequestedAt/deletionReason/deletedAt` · S6 index ของ `AuditLog` · **route:** `/settings/{notifications,line,privacy}`, `/learn/[courseId]/qa[/threadId]`, `/teach/courses/[id]/qa`, `/admin/reviews`, `/admin/deletion-requests`, `/api/cron/cleanup`, `/api/privacy/export` · **dependency:** `recharts` (กราฟหน้า `/admin` เท่านั้น) · **requirement:** FR-17.3 secret อีเมล/LINE อยู่ใน env ไม่เก็บใน DB · FR-17.4 ลบบัญชีต้องให้ Super Admin อนุมัติ | cron ต้องไม่แจ้งซ้ำ · ตอบซ้อน 1 ชั้นต้องมี FK · catalog แสดงดาวโดยไม่ aggregate ทุกการ์ด · PDPA ต้องเก็บสถานะคำขอลบ · เก็บ secret ใน DB ต้องเข้ารหัสและเพิ่มความเสี่ยง | migration 1–2 ไฟล์ตามขั้นที่ใช้ · ยังไม่มี LINE Official Account → ขั้น 2 ทดสอบด้วย webhook จำลอง | อนุมัติแล้ว 2026-09-24 — เจ้าของระบบ |
| 32 | 2026-09-24 | system-design §5.7, §10.1 · `.env.example` · `playwright.config.ts` | (1) env ใหม่ (ไม่บังคับ) `LINE_OA_BASIC_ID` (ลิงก์/QR เพิ่มเพื่อน) และ `LINE_API_URL` (ค่าเริ่มต้น `https://api.line.me`) · (2) `playwright.config.ts` โหลด `.env` (ถ้ามี) · (3) จำกัดการลองรหัสผูก LINE 5 ครั้ง/15 นาทีต่อบัญชี LINE | ยังไม่มี LINE Official Account (Q2) — ทดสอบด้วย webhook จำลองโดยชี้ API ไปพอร์ตที่ไม่มีบริการ และเซ็น webhook ด้วย secret เดียวกับ dev server · กันการเดารหัสของผู้อื่น | ไม่เปลี่ยน schema · e2e `line.spec` ข้ามอัตโนมัติเมื่อไม่ได้ตั้ง LINE ใน `.env` | อยู่ในขอบเขตขั้น 2 ที่อนุมัติ (#31) — แจ้งเจ้าของระบบในรายงานขั้น 2 |
| 33 | 2026-09-25 | phase-3-plan ขั้น 3 · system-design §4.3, §5.7 · `src/lib/env.ts` · `package.json` | (1) key กันแจ้งซ้ำของงานเป็น `due:<assignmentId>:<dueAt>` (แผนเขียน `due:<assignmentId>`) — เลื่อนกำหนดส่งแล้วแจ้งใหม่ตามกำหนดใหม่ เหมือนคาบสด · (2) "ยังไม่ส่ง" รวมผู้ที่งานล่าสุดถูกส่งกลับให้แก้ · (3) `/api/cron/*` ทำเป็น route เดียว `[job]` (URL ตามแผน) รับทั้ง GET และ POST · ไม่ได้ตั้ง `CRON_SECRET` = 404 · (4) env `CRON_SECRET` ตรวจใน `env.ts` (ไม่บังคับ, ≥ 16 ตัวอักษร) · (5) สคริปต์ `pnpm cron <ชื่อ>` · (6) `cleanup` บันทึก AuditLog `cron.cleanup` เมื่อมีการลบ | ผู้สอนเลื่อนกำหนดส่งบ่อย — key ที่ไม่มีเวลาจะไม่เตือนตามกำหนดใหม่ · งานที่ถูกส่งกลับยังต้องส่ง · Vercel Cron เรียกด้วย GET · ลบข้อมูลต้องตรวจย้อนได้ | ไม่เปลี่ยน schema เพิ่มจาก S1 · e2e `cron.spec` ข้ามอัตโนมัติเมื่อไม่ได้ตั้ง `CRON_SECRET` | อยู่ในขอบเขตขั้น 3 ที่อนุมัติ (#31) — แจ้งเจ้าของระบบในรายงานขั้น 3 |
| 34 | 2026-09-25 | Prisma schema (S2) · phase-3-plan ขั้น 4 · system-design §3.2, §4.3 | (1) S2 ใช้ `Thread.editedAt` / `Post.editedAt` (ตั้งเฉพาะเมื่อผู้เขียนแก้เนื้อหา) แทน `updatedAt` · `Thread.lastPostAt` เป็น `DateTime @default(now())` ไม่ใช่ nullable · (2) ผู้สอน/ผู้ดูแลกด "ลบ" = ลบแถวจริงและเก็บสำเนา (หัวข้อ/ข้อความ/ผู้เขียน/จำนวนคำตอบ) ใน AuditLog แทน "ซ่อนถาวร" · (3) เลือกคำตอบที่ดีที่สุดแล้วปิดกระทู้ว่าแก้ไขแล้วอัตโนมัติ · (4) เจ้าของเห็นกระทู้ของตนที่ถูกซ่อน (ติดป้าย) แต่ตอบเพิ่มไม่ได้ · (5) จำกัดการโพสต์ 10 ครั้ง/10 นาทีต่อผู้ใช้ | `@updatedAt` เปลี่ยนเมื่อปักหมุด/ซ่อน/ปิด → ป้าย "แก้ไขแล้ว" ผิด · ซ่อนกับลบต้องแยกกันได้โดยไม่เพิ่มคอลัมน์ และยังมีหลักฐานเมื่อถูกลบ · ลดขั้นตอนของผู้สอน · ผู้ตั้งควรรู้ว่ากระทู้ถูกซ่อน | migration `20260925060000_qa_threads` · ไม่มีตารางใหม่ | อยู่ในขอบเขตขั้น 4 ที่อนุมัติ (#31) — แจ้งเจ้าของระบบในรายงานขั้น 4 |
| 35 | 2026-09-25 | Prisma schema (S3, S4) · phase-3-plan ขั้น 5 · system-design §3.2, §4.3 · seed | (1) แจ้งผู้สอนเมื่อมีรีวิวใหม่ด้วย `NotificationType.SYSTEM` (ไม่เพิ่ม enum) · (2) ผู้เรียนที่สิทธิ์หมดอายุแล้วยังรีวิวได้ถ้าเรียนถึง 30% · ผู้สอนของคอร์สไม่รีวิวคอร์สตัวเอง · (3) ซ่อนรีวิวได้โดย SUPER_ADMIN และ DEPT_ADMIN ของคณะเจ้าของคอร์ส (สิทธิ์ `manage`) · ผู้สอนตอบกลับได้ (`teach`) · ตอบกลับว่าง = ลบ · (4) catalog เลิก aggregate รีวิวต่อหน้า ใช้ค่าที่เก็บบนคอร์ส และเรียงด้วย DB · (5) seed ผู้เรียนสาธิต 2 บัญชี **ไม่มีรหัสผ่าน** (ล็อกอินไม่ได้) พร้อมรีวิว/กระทู้ตัวอย่างใน `assessment-demo` | ไม่แก้ schema เพิ่มเพื่อชนิดแจ้งเตือน · FR-14.1 วัดที่ "ได้เรียนจริง" · ขอบเขตผู้ดูแลเหมือนการอนุมัติคอร์ส · catalog ไม่ช้าลงเมื่อคอร์สมากขึ้น · สาธิตได้ทันทีโดยไม่เพิ่มบัญชีที่ล็อกอินได้ | migration `20260925090000_review_rating` (CHECK rating 1–5 + คำนวณค่าเฉลี่ยจากรีวิวเดิม) | อยู่ในขอบเขตขั้น 5 ที่อนุมัติ (#31) — แจ้งเจ้าของระบบในรายงานขั้น 5 |
| 36 | 2026-09-25 | phase-3-plan ขั้น 6 · `package.json` (recharts ตาม #31) · เมนูแอดมิน | (1) นิยามอัตราการเรียนจบ = COMPLETED ÷ (ACTIVE + COMPLETED + EXPIRED) ไม่นับรออนุมัติ/ถอนตัว · (2) ช่วงวันที่ของรายงานกรองตาม**วันที่ลงทะเบียน** (เวลาไทย) · (3) ไฟล์ส่งออกสูงสุด 10,000 แถว และเติม `'` หน้าข้อความที่ขึ้นต้นด้วย = + - @ (กันสูตรใน Excel) · (4) index `AuditLog` (S6) เลื่อนไปขั้น 7 · (5) แดชบอร์ดแอดมินแทนการ์ดสถานะบัญชีด้วยตัวเลขตาม FR-16.3 · ลบเมนู "การประเมินผล (เฟส 2)" ที่ไม่มีหน้าจริง เพิ่มเมนู "รายงาน" | FR-16 ไม่ได้นิยามสูตร · ไฟล์ที่ผู้ใช้กรอกชื่อเองเปิดใน Excel ต้องไม่รันสูตร · S6 ใช้กับหน้าค้นหา audit ซึ่งอยู่ขั้น 7 · เมนูที่กดแล้ว 404 | ไม่เปลี่ยน schema · dependency ใหม่ `recharts` โหลดแบบ lazy เฉพาะ `/admin` | อยู่ในขอบเขตขั้น 6 ที่อนุมัติ (#31) — แจ้งเจ้าของระบบในรายงานขั้น 6 |
| 37 | 2026-09-25 | Prisma schema (S5, S6) · phase-3-plan ขั้น 7 · system-design §3.3, §4.3, §11 · spec FR-17 | (1) migration S5 `User.deletionRequestedAt/deletionReason/deletedAt` + S6 index `AuditLog` ตามที่อนุมัติ (#31) · (2) `writeAudit()` ซ่อนค่าฟิลด์ชื่อ password/token/secret/apiKey/otp เป็น `[ซ่อน]` ทุกครั้ง · บันทึก `user.register` และ `user.password.change` ผ่าน database hook ของ Better Auth (ไม่มีค่ารหัสผ่าน) · (3) **สวิตช์การป้องกันระดับระบบย้ายไป `/admin/settings`** หน้า `/admin/screen-events` เหลือแค่สถานะ + ลิงก์ · (4) ชื่อระบบ/โลโก้เก็บใน `SystemSetting` key `branding` · โลโก้ต้องเป็นรูปที่ผู้ดูแลคนนั้นอัปโหลดเอง · (5) บัญชีที่เข้าด้วย Google อย่างเดียวไม่มีรหัสผ่าน → ยืนยันคำขอลบด้วยการพิมพ์อีเมลของตนเอง · (6) ผลอนุมัติ/ปฏิเสธส่งอีเมลเสมอ (เป็นอีเมลเกี่ยวกับบัญชี เหมือนลิงก์รีเซ็ตรหัสผ่าน ไม่ขึ้นกับการตั้งค่าแจ้งเตือน) + ปฏิเสธแจ้งในแอปด้วย · (7) anonymize: ลบ session/account/LINE/แจ้งเตือน/เหตุการณ์หน้าจอ · ลบไฟล์ PDF ใบประกาศเดิม (มีชื่อจริง) แล้วสร้างใหม่เมื่อมีคนขอ · ล้างชื่อ/อีเมล/เบอร์/รหัสใน `AuditLog.before/after` และ IP ของการกระทำ · **งานที่ส่ง (ไฟล์) กระทู้ และรีวิวคงไว้** เป็นประวัติการเรียนแบบไม่ระบุตัวตน · บัญชีที่ลบแล้วไม่แสดงใน `/admin/users` และไม่นับในสถิติผู้ใช้ | FR-17.1 ห้ามเก็บ secret ใน audit แต่ไม่มีจุดบังคับกลาง · สมัคร/เปลี่ยนรหัสผ่านไม่ผ่าน actions.ts · แผนระบุ "ย้ายจากที่เดิม" · Q9 ต้องยืนยันตัวตน แต่บัญชี Google ไม่มีรหัสผ่าน · หลัง anonymize ไม่มีอีเมลจริงให้แจ้ง · PDF ที่เก็บไว้ฝังชื่อจริง | ไม่เพิ่มตาราง/dependency · route ใหม่ `/admin/{audit,settings,deletion-requests}`, `/settings/privacy`, `/api/privacy/export` ตาม §5 ของแผน · (7) เจ้าของระบบยืนยันให้คงไฟล์งานที่ส่ง (2026-09-25) | อยู่ในขอบเขตขั้น 7 ที่อนุมัติ (#31) — แจ้งเจ้าของระบบในรายงานขั้น 7 |
| 38 | 2026-09-25 | docs/phase-4-plan.md · spec §6, §7 (D-05) · Prisma schema (S1–S6) · system-design §4.3 · tech stack | เพิ่มแผน Phase 4 (ขั้น 0 + 6 ขั้น · DRM ไม่ทำ) · **ตัดสินใจ:** Q1 gateway = Opn Payments (Omise) ผ่าน adapter `lib/payment` + mock provider สำหรับ dev/e2e · Q2 ตั้งต้นออกใบเสร็จรับเงินอย่างเดียว (รอฝ่ายบัญชียืนยันเรื่อง VAT/ใบกำกับภาษี) · Q3 ผู้สอนเสนอราคา มีผลเมื่อผู้ดูแลอนุมัติ · คูปองเฉพาะผู้ดูแล · ผู้สอนไม่เห็นยอดเงิน · Q4 ขายเฉพาะคอร์ส PUBLIC · Q5 ซื้อแล้วเรียนได้ตลอด · Q6 คืนเงินเต็มจำนวนภายใน 7 วันและเรียน ≤ 20% · คืนแล้วยกเลิกสิทธิ์ + เพิกถอนใบประกาศ · Q7 ยอดล็อกตอนสร้างคำสั่งซื้อ 30 นาที · Q8 ไม่ทำ DRM · **schema:** S1 เติมคอลัมน์ `Order` · S2 partial unique PENDING ต่อ `[userId, courseId]` · S3 `Coupon.id` แยกจาก code + CHECK · S4 ตาราง `PaymentEvent` · S5 ตาราง `DocumentCounter` · S6 CHECK `Course.price >= 0` · **route:** `/checkout/[courseId]`, `/checkout/mock/[orderId]`, `/orders[/orderId]`, `/admin/{orders,coupons}`, `/api/payment/webhook/[provider]`, `/api/receipt/[orderId]` · env `PAYMENT_*` (ไม่บังคับ) | spec §6 Phase 4 = M18 · D-05 ค้างมาตั้งแต่ baseline | ตารางใหม่ 2 · route ใหม่ 7 · dependency ของ gateway (ถ้าจำเป็น) · จุดตรวจ 2 จุด | อนุมัติแล้ว — เจ้าของระบบ 2026-09-25 ("ใช้ตามข้อเสนอทั้งหมด") |
| 39 | 2026-09-25 | phase-4-plan ขั้น 2 · system-design §4.3 · `.github/workflows/ci.yml` · `.env.example` | (1) งาน cron ใหม่ `/api/cron/orders` (ทุก 15 นาที) ปิดคำสั่งซื้อ PENDING ที่หมดอายุ — แผนเขียนให้อยู่ใน cleanup · (2) ตัวเชื่อม Omise เลื่อนไปทำพร้อมจุดตรวจที่ 1 เมื่อได้ sandbox · (3) CI ตั้ง `PAYMENT_PROVIDER=mock` + `ALLOW_MOCK_PAYMENT=true` สำหรับ e2e · (4) เริ่มชำระซ้ำ = ปิดคำสั่งซื้อค้างเดิม (หลังถามผล) แล้วสร้างใหม่ — charge เก่ายังเปิดสิทธิ์ได้ถ้าจ่ายตามมา (FAILED → PAID) | cleanup รันวันละครั้ง ช้าเกินอายุคำสั่งซื้อ 30 นาที · โค้ดที่ทดสอบกับของจริงไม่ได้เสี่ยงผิด · e2e ต้องชำระเงินได้ · ไม่ทิ้งเงินที่จ่ายแล้วโดยไม่ได้สิทธิ์ | route cron เพิ่ม 1 · ผู้ซื้ออาจจ่ายซ้ำสองคำสั่งซื้อได้ในกรณีหายาก → ผู้ดูแลคืนเงิน (ขั้น 5) | อยู่ในขอบเขตขั้น 2 ที่อนุมัติ (#38) — แจ้งเจ้าของระบบในรายงานขั้น 2 |
| 40 | 2026-09-26 | phase-4-plan ขั้น 3 · system-design §4.3 | คูปอง (FR-18.2): (1) `/admin/coupons` เฉพาะ SUPER_ADMIN · (2) ไม่มีลบ/แก้คูปอง — ปิดใช้แล้วสร้างใหม่ · (3) จองสิทธิ์ (Q7) โดยนับคำสั่งซื้อ PENDING ที่ยังไม่หมดอายุ + ล็อกแถวคูปอง ไม่ใช่ตัด `usedCount` ตอนสร้าง · `usedCount` เพิ่มตอนชำระสำเร็จและอาจเกิน `maxUses` เมื่อจ่ายหลังคำสั่งซื้อหมดอายุ · (4) ยอด 0 บาทบันทึกเป็นคำสั่งซื้อ PAID `method = coupon` · (5) ไม่จำกัดจำนวนครั้งต่อผู้ใช้ · เจ้าของระบบให้ข้าม Q1 (sandbox) / Q2 (VAT) ไว้ก่อน | Q3 ระบุ "ผู้ดูแล" ไม่ชัดว่ารวม DEPT_ADMIN — คูปองทุกคอร์สข้ามคณะ · คำสั่งซื้อเก่าอ้างถึงคูปอง · ไม่ต้องคืนสิทธิ์เมื่อคำสั่งซื้อหมดอายุ/ล้มเหลว (ไม่ต้องแก้ cron) · เงินเข้าแล้วต้องได้สิทธิ์ · แผนไม่ได้ระบุ | route เพิ่ม 1 (อยู่ใน §5 แล้ว) · ไม่เปลี่ยน schema · ถ้าต้องการให้ DEPT_ADMIN สร้างคูปองของคณะตน/จำกัดต่อผู้ใช้ เพิ่มได้ภายหลัง | อยู่ในขอบเขตขั้น 3 ที่อนุมัติ (#38) — แจ้งเจ้าของระบบในรายงานขั้น 3 |
| 41 | 2026-09-26 | phase-4-plan ขั้น 4 · system-design §4.3 · `CHECKOUT_QUOTA` | ใบเสร็จรับเงิน (FR-18.2): (1) Q2 ยังไม่มีคำตอบ → ใบเสร็จรับเงินอย่างเดียว ไม่มี VAT/ข้อมูลผู้เสียภาษีของผู้ซื้อ · (2) ไม่เก็บไฟล์ PDF — สร้างจาก snapshot ผู้ขาย/ผู้ซื้อใน `Order.billing` ทุกครั้ง · (3) คำสั่งซื้อ 0 บาท (คูปอง 100%) ไม่ออกใบเสร็จ · (4) แจ้งเตือน/อีเมลชำระสำเร็จลิงก์ไป `/orders/[id]` (มีปุ่มใบเสร็จ) แทน `/learn/[id]` · (5) `CHECKOUT_QUOTA` 10 → 30 ครั้ง/10 นาที | เจ้าของระบบสั่งข้าม Q2 ไว้ก่อน · เอกสารเดิมต้องไม่เปลี่ยนเมื่อแก้ข้อมูลผู้ขาย และไม่ต้องดูแลไฟล์ค้างใน storage · ไม่มีการรับเงิน · แผนให้ "แนบลิงก์ในอีเมล" · e2e ทุกไฟล์ซื้อด้วยบัญชีผู้เรียนเดียวกันเกินโควตาเดิม | route `/api/receipt/[orderId]` (อยู่ใน §5 แล้ว) · ไม่เปลี่ยน schema · ถ้า Q2 ต้องออกใบกำกับภาษี: เพิ่มช่องผู้ซื้อที่ checkout + VAT + เลขเอกสารชุดใหม่ | อยู่ในขอบเขตขั้น 4 ที่อนุมัติ (#38) — แจ้งเจ้าของระบบในรายงานขั้น 4 |
| 42 | 2026-09-26 | phase-4-plan ขั้น 5 · system-design §4.3 | คืนเงิน (FR-18.2 · Q6): (1) `/admin/orders` และการคืนเงินเฉพาะ SUPER_ADMIN · (2) ยังไม่ออกใบลดหนี้/เอกสารคืนเงิน (รอ Q2) — ใบเสร็จเดิมคงไว้และดาวน์โหลดได้ · (3) คำสั่งซื้อ 0 บาทคืนเงินไม่ได้ · (4) "รอผู้ให้บริการยืนยันการคืนเงิน" = PAID + `refundReason` มีค่า แทนสถานะใหม่ · (5) แจ้งเตือนคืนเงินใช้ชนิด ENROLLED · (6) เพิ่มปุ่ม "ตรวจสอบกับผู้ให้บริการ" (`recheckOrder()`) ตามแนวทางความเสี่ยงใน §7 | สอดคล้องกับคูปอง (#40) — เรื่องเงินให้ผู้ดูแลระบบกลาง · เจ้าของระบบสั่งข้าม Q2 · ไม่มีเงินให้คืน · ไม่ต้องเปลี่ยน enum `OrderStatus`/`NotificationType` (schema) · ชนิด ENROLLED เปิดอีเมลเป็นค่าเริ่มต้นและเป็นหมวดการลงทะเบียน | route `/admin/orders` (อยู่ใน §5 แล้ว) · ไม่เปลี่ยน schema · ถ้า Q2 ต้องมีใบลดหนี้: เพิ่มเลขเอกสารชุดใหม่ใน `DocumentCounter` และ PDF อีกแบบ | อยู่ในขอบเขตขั้น 5 ที่อนุมัติ (#38) — แจ้งเจ้าของระบบในรายงานขั้น 5 |
